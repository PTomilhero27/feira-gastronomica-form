import FairsPage from "./components/fairs-page";

/**
 * Página /feiras (Portal do Expositor).
 *
 * Esta página é responsável por renderizar a tela principal de feiras do expositor,
 * delegando a lógica de dados e UI para um componente client (TanStack Query).
 */
export default function Page() {
  return <FairsPage />
}
